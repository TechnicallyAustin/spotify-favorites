Create a new form if you can let their values be the paramsa for the current playlist, instead of a patch changing the entire project i will use a puts, this way it keeps the Id the same and allows the user to make modular changes


<form action='POST' method='/playlist/'>